namespace Acheve.Authentication.Events
{
    internal class Constants
    {
        public const string LocalAuthenticatuionHandler = "Non Remote Authentication Handler";
        public const string RemoteAuthenticatuionHandler = "Remote Authentication Handler";
    }
}
